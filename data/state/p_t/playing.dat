Hooks:
  Received:-
    local instance = d:get_character();
    if instance then
      if line:match("^%s*$") then
        line = "look";
      end
      dispatch_command(instance, line);
    end
    ~
  ~
Name: Playing~
StateBits:
  Prompt: Yes~
  ~
~
