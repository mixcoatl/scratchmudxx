Hooks:
  Focus:-
    require("io").prompt(d, Q, "Enter the connection state's name");
    ~
  Received:-
    local edit = d:get_edit_state();
    if not edit then
      d:pop_state();
    else
      local name = line:match("%S+") or "";
      local original = d:get_edit_name();
      local existing = get_states():get(name);
      if name == "" then
        require("io").fail(d, Q, "Name entry cancelled.");
        d:pop_state();
      elseif existing and (original == "" or original:lower() ~~= name:lower()) then
        require("io").fail(d, Q, "That state name is already taken.");
        d:set_state("StateName");
      else
        edit:set_name(name);
        d:pop_state();
      end
    end
    ~
  ~
Name: StateName~
~
